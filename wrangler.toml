name = "foodbook"
compatibility_date = "2026-05-13"
pages_build_output_dir = "./dist"

# Static-only deploy — no bindings required today. If/when an SSR
# surface from `docs/feature-wishlist.md` lands, wire its binding
# here and add the matching Pages secret:
#   wrangler pages secret put <KEY> --project-name=foodbook
